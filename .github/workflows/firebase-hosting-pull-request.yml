# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on PR
'on': pull_request
env:
  API_KEY: ${{secrets.ANGULAR_API_KEY}}
  APP_ID: ${{secrets.ANGULAR_APP_ID}}
  AUTH_DOMAIN: ${{secrets.ANGULAR_AUTH_DOMAIN}}
  DATABASE_URL: ${{secrets.ANGULAR_DATABASE_URL}}
  MEASUREMENT_ID: ${{secrets.ANGULAR_MEASUREMENT_ID}}
  MESSAGING_SENDER_ID: ${{secrets.ANGULAR_MESSAGING_SENDER_ID}}
  PROJECT_ID: ${{secrets.ANGULAR_PROJECT_ID}}
  STORAGE_BUCKET: ${{secrets.ANGULAR_STORAGE_BUCKET}}

jobs:
  build_and_preview:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_COVID2_36557 }}'
          projectId: covid2-36557
        env:
          FIREBASE_CLI_PREVIEWS: hostingchannels
